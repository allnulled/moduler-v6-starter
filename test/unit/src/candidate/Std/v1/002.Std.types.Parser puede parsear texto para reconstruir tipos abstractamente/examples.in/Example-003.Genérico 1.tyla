{
    name?: string,
    password: string,
    email?: string,
    hobbies?: [{
        name: string,
        frequency: option(["low","medium","high"])
    }],
    city?: option.predefined("cities of the world")? & available cities
}